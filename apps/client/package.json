{
  "name": "@hall-of-fame/client",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:h5": "tsx src/dev-h5.ts",
    "dev:weapp": "tsx src/dev-weapp.ts",
    "build": "tsc -p tsconfig.json",
    "test": "node --import tsx --test src/**/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hall-of-fame/api-client": "workspace:*",
    "fastify": "^5.4.0",
    "react": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.2"
  }
}
